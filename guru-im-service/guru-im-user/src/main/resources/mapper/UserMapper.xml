<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.guru.im.user.mapper.UserMapper">
    <select id="getByUserName" resultType="com.guru.im.user.model.pojo.User">
        SELECT *
        FROM im_user
        WHERE user_name = #{userName}
    </select>
    <select id="getByUid" resultType="com.guru.im.user.model.pojo.User">
        SELECT * FROM im_user WHERE uid = #{uid}
    </select>

    <select id="queryAllUsers" resultType="com.guru.im.user.model.pojo.User">
        SELECT * FROM im_user
    </select>

    <select id="queryByUserName" resultType="com.guru.im.user.model.pojo.User">
        SELECT * FROM im_user where user_name like  CONCAT('%', #{userName}, '%')
    </select>


    <insert id="save" parameterType="com.guru.im.user.model.pojo.User" >
        INSERT INTO im_user
            (uid, user_name, password)
        VALUES
            (#{uid}, #{userName}, #{password})
    </insert>
</mapper>